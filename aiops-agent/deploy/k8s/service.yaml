apiVersion: v1
kind: Service
metadata:
  name: aiops-agent
  namespace: aiops
spec:
  type: ClusterIP
  selector:
    app: aiops-agent
  ports:
    - name: http
      port: 18080
      targetPort: 18080
