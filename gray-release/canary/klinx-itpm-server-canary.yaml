apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-itpm-server
  namespace: saas
spec:
  hosts:
    - klinx-itpm-server
  http:
    - route:
        - destination:
            host: klinx-itpm-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-itpm-server
            subset: v2
          weight: 5