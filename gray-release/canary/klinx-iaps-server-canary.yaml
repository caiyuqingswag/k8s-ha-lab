apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-iaps-server
  namespace: saas
spec:
  hosts:
    - klinx-iaps-server
  http:
    - route:
        - destination:
            host: klinx-iaps-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-iaps-server
            subset: v2
          weight: 5