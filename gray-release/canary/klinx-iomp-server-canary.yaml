apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-iomp-server
  namespace: saas
spec:
  hosts:
    - klinx-iomp-server
  http:
    - route:
        - destination:
            host: klinx-iomp-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-iomp-server
            subset: v2
          weight: 5