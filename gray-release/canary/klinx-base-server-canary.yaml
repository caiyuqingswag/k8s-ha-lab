apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-base-server
  namespace: saas
spec:
  hosts:
    - klinx-base-server
  http:
    - route:
        - destination:
            host: klinx-base-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-base-server
            subset: v2
          weight: 5