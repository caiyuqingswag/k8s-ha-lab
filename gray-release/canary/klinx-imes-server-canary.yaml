apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-imes-server
  namespace: saas
spec:
  hosts:
    - klinx-imes-server
  http:
    - route:
        - destination:
            host: klinx-imes-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-imes-server
            subset: v2
          weight: 5