apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: klinx-datacollect-server
  namespace: saas
spec:
  hosts:
    - klinx-datacollect-server
  http:
    - route:
        - destination:
            host: klinx-datacollect-server
            subset: v1
          weight: 95
        - destination:
            host: klinx-datacollect-server
            subset: v2
          weight: 5