apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-bizbase-server-v1
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-bizbase-server
      version: v1
  template:
    metadata:
      labels:
        app: klinx-bizbase-server
        version: v1
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 20
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - klinx-bizbase-server
                topologyKey: kubernetes.io/hostname
      containers:
        - name: klinx-bizbase-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-bizbase-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 6144Mi
          ports:
            - name: http-8751
              containerPort: 8751
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8751
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8751
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 1
            periodSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8751
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 20
      initContainers:
        - name: init
          image: busybox:1.36
          command:
            - sh
          args:
            - "-c"
            - until nc -vz klinx-base-server 8750; do echo waiting for base; sleep 2; done
      