apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-iaps-server-v1
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-iaps-server
      version: v1
  template:
    metadata:
      labels:
        app: klinx-iaps-server
        version: v1
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - klinx-iaps-server
                topologyKey: kubernetes.io/hostname
      containers:
        - name: klinx-iaps-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-iaps-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: DISCOVERY_PORT
              value: "8713"
            - name: SERVER_PORT
              value: "8713"
            - name: JAVA_OPTS
              value: "-XX:MaxRAMPercentage=85.0"
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 3072Mi
          ports:
            - name: tcp-8713
              containerPort: 8713
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8713
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8713
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 1
            periodSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8713
              scheme: HTTP
            initialDelaySeconds: 15
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 40
      initContainers:
        - name: init
          image: busybox:latest
          command:
            - sh
          args:
            - "-c"
            - until nc -vz klinx-iccs-server 8707; do echo waiting for ccs; sleep 2; done
      dnsPolicy: ClusterFirst