apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-itex-cloud
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-itex-cloud
  template:
    metadata:
      labels:
        app: klinx-itex-cloud
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 5
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - klinx-itex-cloud
                topologyKey: kubernetes.io/hostname
      containers:
        - name: klinx-itex-cloud
          image: node5:443/itex-biz/klinx-itex-portal:private_jingyuan_qa_sit
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          ports:
            - name: http-80
              containerPort: 80
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 120
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 3
            successThreshold: 1
            failureThreshold: 3
      initContainers:
        - name: init
          image: busybox:1.36
          command: ["sh","-c"]
          args:
            - until nc -vz klinx-openapi-server 8712;do echo waiting for gateway;sleep 2;done
      dnsPolicy: ClusterFirst
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-itex-cloud
  namespace: saas
spec:
  type: NodePort
  selector:
    app: klinx-itex-cloud
  ports:
    - name: http-80
      port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30868
