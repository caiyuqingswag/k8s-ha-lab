apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-iems-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-iems-server
  template:
    metadata:
      labels:
        app: klinx-iems-server
    spec:
      tolerations:
        - key: "node.kubernetes.io/not-ready"
          operator: "Exists"
          effect: "NoExecute"
          tolerationSeconds: 60
        - key: "node.kubernetes.io/unreachable"
          operator: "Exists"
          effect: "NoExecute"
          tolerationSeconds: 60
      terminationGracePeriodSeconds: 5
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - klinx-iems-servers
                topologyKey: kubernetes.io/hostname
      containers:
        - name: klinx-iems-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-iems-server:private_jingyuan_qa_sit
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              memory: 6144Mi
          ports:
            - name: tcp-8703
              containerPort: 8703
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8703
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8703
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 1
            periodSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8703
              scheme: HTTP
            timeoutSeconds: 1
            successThreshold: 1
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      dnsPolicy: ClusterFirst
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
apiVersion: v1
kind: Service
metadata:
  name: klinx-iems-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-iems-server
  ports:
    - name: tcp-8703
      port: 8703
      protocol: TCP
      targetPort: 8703