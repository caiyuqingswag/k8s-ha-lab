apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-iwms-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-iwms-server
  template:
    metadata:
      labels:
        app: klinx-iwms-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: klinx-iwms-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-iwms-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 300m
              memory: 800Mi
            limits:
              cpu: "1"
              memory: 2Gi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: root-logs
              mountPath: /root/logs
            - name: app-logs
              mountPath: /Users/texpro/data/projects/logs/klinx-iwms-server
            - name: heapdump
              mountPath: /heapdump
          ports:
            - name: tcp-8706
              containerPort: 8706
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: 8706
            initialDelaySeconds: 60
            periodSeconds: 6
            failureThreshold: 20
          readinessProbe:
            tcpSocket:
              port: 8706
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: { }
        - name: root-logs
          emptyDir: { }
        - name: app-logs
          emptyDir: { }
        - name: heapdump
          persistentVolumeClaim:
            claimName: heapdump-pvc      
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-iwms-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-iwms-server
  ports:
    - name: tcp-8706
      port: 8706
      protocol: TCP
      targetPort: 8706