apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-iagv-server
  namespace: saas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: klinx-iagv-server
  template:
    metadata:
      labels:
        app: klinx-iagv-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: klinx-iagv-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-iagv-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 300m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1800Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: root-logs
              mountPath: /root/logs
            - name: app-logs
              mountPath: /Users/texpro/data/projects/logs/klinx-iagv-server
            - name: heapdump
              mountPath: /heapdump
          ports:
            - name: tcp-8718
              containerPort: 8718
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8718
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 6
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8718
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 2
            periodSeconds: 2
            successThreshold: 2
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8718
              scheme: HTTP
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: { }
        - name: root-logs
          emptyDir: { }
        - name: app-logs
          emptyDir: { }
        - name: heapdump
          persistentVolumeClaim:
            claimName: heapdump-pvc      
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-iagv-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-iagv-server
  ports:
    - name: tcp-8718
      port: 8718
      protocol: TCP
      targetPort: 8718