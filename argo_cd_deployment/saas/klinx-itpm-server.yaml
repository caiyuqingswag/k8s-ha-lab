apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-itpm-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-itpm-server
  template:
    metadata:
      labels:
        app: klinx-itpm-server
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - klinx-itpm-servers
                topologyKey: kubernetes.io/hostname
      containers:
        - name: klinx-itpm-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-itpm-server:private_jingyuan_qa_sit
          env:
            - name: JAVA_OPTS
              value: -XX:MaxRAMPercentage=85.0
            - name: DISCOVERY_PORT
              value: "8716"               # 提供给 ${DISCOVERY_PORT}
            - name: SERVER_PORT
              value: "8716"               # 如果配置里用了 ${server.port}
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 2000m
              memory: 3072Mi
          ports:
            - name: tcp-8716
              containerPort: 8716
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8716
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8716
              scheme: HTTP
            initialDelaySeconds: 6
            timeoutSeconds: 1
            periodSeconds: 2
            successThreshold: 1
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8716
              scheme: HTTP
            initialDelaySeconds: 15
            timeoutSeconds: 1
            periodSeconds: 6
            successThreshold: 1
            failureThreshold: 40
      initContainers:
        - name: init
          image: busybox:latest
          command:
            - sh
          args:
            - '-c'
            - until nc -vz klinx-iccs-server 8707;do echo waiting for ccs;sleep 2;done
      dnsPolicy: ClusterFirst
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: "25%"
      maxSurge: "25%"
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
apiVersion: v1
kind: Service
metadata:
  name: klinx-itpm-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-itpm-server
  ports:
    - name: tcp-8716
      port: 8716
      protocol: TCP
      targetPort: 8716
      #nodePort: 32543
