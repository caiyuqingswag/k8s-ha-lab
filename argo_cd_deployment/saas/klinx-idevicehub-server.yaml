---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-idevicehub-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-idevicehub-server
  template:
    metadata:
      labels:
        app: klinx-idevicehub-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: klinx-idevicehub-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-idevicehub-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "1"
              memory: 3Gi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: root-logs
              mountPath: /root/logs
            - name: app-logs
              mountPath: /Users/texpro/data/projects/logs/klinx-idevicehub-server
            - name: heapdump
              mountPath: /heapdump
          ports:
            - name: tcp-8710
              containerPort: 8710
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8710
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 6
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8710
              scheme: HTTP
            initialDelaySeconds: 6
            periodSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8710
              scheme: HTTP
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: { }
        - name: root-logs
          emptyDir: { }
        - name: app-logs
          emptyDir: { }
        - name: heapdump
          persistentVolumeClaim:
            claimName: heapdump-pvc      
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-idevicehub-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-idevicehub-server
  ports:
    - name: tcp-8710
      port: 8710
      protocol: TCP
      targetPort: 8710