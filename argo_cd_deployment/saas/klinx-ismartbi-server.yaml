
apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-ismartbi-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-ismartbi-server
  template:
    metadata:
      labels:
        app: klinx-ismartbi-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: klinx-ismartbi-server
          imagePullPolicy: Always
          image: node5:443/itex-biz/klinx-ismartbi-server:private_jingyuan_qa_sit
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 300m
              memory: 800Mi
            limits:
              cpu: "1"
              memory: 2Gi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: root-logs
              mountPath: /root/logs
            - name: app-logs
              mountPath: /Users/texpro/data/projects/logs/klinx-ismartbi-server
            - name: heapdump
              mountPath: /heapdump
          ports:
            - name: tcp-8708
              containerPort: 8708
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8708
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 6
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8708
              scheme: HTTP
            initialDelaySeconds: 6
            periodSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8708
              scheme: HTTP
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: { }
        - name: root-logs
          emptyDir: { }
        - name: app-logs
          emptyDir: { }
        - name: heapdump
          persistentVolumeClaim:
            claimName: heapdump-pvc      
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-ismartbi-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-ismartbi-server
  ports:
    - name: tcp-8708
      port: 8708
      protocol: TCP
      targetPort: 8708