apiVersion: apps/v1
kind: Deployment
metadata:
  name: klinx-datahub-server
  namespace: saas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: klinx-datahub-server
  template:
    metadata:
      labels:
        app: klinx-datahub-server
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: klinx-datahub-server
          image: node5:443/itex-biz/klinx-datahub:private_hanew
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          envFrom:
            - configMapRef:
                name: saas-config
          resources:
            requests:
              cpu: 500m
              memory: 500Mi
            limits:
              cpu: 800m
              memory: 1800Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: root-logs
              mountPath: /root/logs
            - name: app-logs
              mountPath: /Users/texpro/data/projects/logs/klinx-datahub-server
            - name: heapdump
              mountPath: /heapdump
          ports:
            - name: tcp-9560
              containerPort: 9560
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9560
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 6
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9560
              scheme: HTTP
            initialDelaySeconds: 6
            periodSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9560
              scheme: HTTP
            initialDelaySeconds: 6
            failureThreshold: 30
            periodSeconds: 10
      volumes:
        - name: tmp
          emptyDir: { }
        - name: root-logs
          emptyDir: { }
        - name: app-logs
          emptyDir: { }
        - name: heapdump
          persistentVolumeClaim:
            claimName: heapdump-pvc      
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
---
apiVersion: v1
kind: Service
metadata:
  name: klinx-datahub-server
  namespace: saas
spec:
  type: ClusterIP
  selector:
    app: klinx-datahub-server
  ports:
    - name: tcp-9560
      port: 9560
      protocol: TCP
      targetPort: 9560