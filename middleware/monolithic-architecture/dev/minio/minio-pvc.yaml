apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-minio-dev
  namespace: dev-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: minio-nfs-dev
  resources:
    requests:
      storage: 500Gi
  volumeName: pv-minio-dev