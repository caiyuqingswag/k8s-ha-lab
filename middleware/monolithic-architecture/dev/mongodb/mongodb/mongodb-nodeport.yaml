apiVersion: v1
kind: Service
metadata:
  name: mongodb-nodeport
  namespace: dev-basic
  labels:
    app: mongodb
spec:
  type: NodePort
  selector:
    app: mongodb
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
      nodePort: 31017