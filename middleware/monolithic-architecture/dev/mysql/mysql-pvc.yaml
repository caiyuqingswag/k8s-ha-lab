apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-mysql
  namespace: dev-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mysql-nfs-static
  resources:
    requests:
      storage: 200Gi
  volumeName: pv-mysql