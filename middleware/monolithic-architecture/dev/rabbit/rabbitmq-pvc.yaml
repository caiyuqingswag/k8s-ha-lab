apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-rabbitmq
  namespace: dev-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rabbitmq-nfs-static
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-rabbitmq