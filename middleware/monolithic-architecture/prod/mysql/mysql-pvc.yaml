apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-mysql-prod
  namespace: prod-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: mysql-nfs-prod
  resources:
    requests:
      storage: 200Gi
  volumeName: pv-mysql-prod