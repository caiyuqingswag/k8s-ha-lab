apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-minio-prod
  namespace: prod-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: minio-nfs-prod
  resources:
    requests:
      storage: 500Gi
  volumeName: pv-minio-prod