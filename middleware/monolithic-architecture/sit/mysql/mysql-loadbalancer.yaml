apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: sit-basic
  annotations:
    kube-vip.io/loadbalancer: "true"
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  sessionAffinity: None
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mysql
