apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis
  namespace: basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: redis-nfs-static
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-redis