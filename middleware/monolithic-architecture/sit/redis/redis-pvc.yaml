apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis-sit
  namespace: sit-basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: redis-nfs-sit
  resources:
    requests:
      storage: 50Gi
  volumeName: pv-redis-sit