apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-minio
  namespace: basic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: minio-nfs-static
  resources:
    requests:
      storage: 500Gi
  volumeName: pv-minio